public class Main {
    public static void main(String[] args) {
/*
        // Setup for time complexity testing
        for (int i = 1; i <= 11; i++) {
            Testing testing = new Testing();
            testing.createAllInputs(16, 10000, 2, 2500);
            testing.runTest("timeDataBinaryWorst" + i + ".txt");
        }
*/

/*
        // Setup for stability testing
        Testing testing = new Testing();
        Element[] elements = Element.createArrayOfRandom((int) Math.pow(2, 10), 2500);
        for (int i = 0; i <= 4; i++) {
            testing.stabilityTest(elements, i);
        }
*/
    }
}
